<?php
// Copy this file to config.local.php and fill in your values
// This file should NOT be committed to version control

// Database
// define('DB_HOST', 'localhost');
// define('DB_NAME', 'familyboard');
// define('DB_USER', 'your_user');
// define('DB_PASS', 'your_password');

// Debug (ne JAMAIS activer en production : affiche le détail des erreurs aux visiteurs)
// define('APP_DEBUG', true);

// Clé de chiffrement applicative (secrets 2FA au repos). Recommandée en production —
// sans elle une clé est générée automatiquement dans storage/app_key.bin (fonctionne,
// mais ne survit pas à une suppression de storage/). Générer par exemple avec :
//   php -r "echo bin2hex(random_bytes(32)), PHP_EOL;"
// define('APP_ENCRYPTION_KEY', 'votre-clé-générée');
